초기 설정

brew.sh 에서 homebrew 설치
homebrew에서 

$ brew install fish
$ fish_add_path /opt/homebrew/bin
$ echo "/opt/homebrew/bin/fish" | sudo tee -a /etc/shells #fish를 온전한 쉘로 인식할 수 있게 기존 쉘 경로로 복사해주눈 과정?
$ chsh -s /opt/homebrew/bin/fish  #fish를 기본 쉘로 변경


